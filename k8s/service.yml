apiVersion: v1
kind: Service
metadata:
  name: app-data-consumer-service
spec:
  type: LoadBalancer
  selector:
    app: app-data-consumer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080